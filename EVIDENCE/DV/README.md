# EVIDENCE

Артефакты для DV4:

- `docker-image.txt` — размер образа и история слоёв `wishlist-api:local`.
- `docker-compose-up.txt` — фрагмент лога успешного запуска `docker compose up --build`.
- `pytest-local.txt` — успешный локальный прогон `pytest -q`.
- `ci-2025-12-10-build.txt` — лог успешного запуска GitHub Actions workflow `CI` на ветке `main`.
